
// Global state object shared across all modules and this will share its data
export const state = {
    menu: [], //menu items fetch from api
    cart: [], //items the user adds to cart
    order: null //stores data returned from the api after placing an order
}